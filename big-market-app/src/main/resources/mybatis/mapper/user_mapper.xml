<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.persistent.dao.IUserDao">

    <resultMap id="userMap" type="cn.bugstack.infrastructure.persistent.po.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insert" parameterType="cn.bugstack.infrastructure.persistent.po.User">
        INSERT INTO user (user_id, username, password, name, email, phone, status, create_time, update_time)
        VALUES (#{userId}, #{username}, #{password}, #{name}, #{email}, #{phone}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="queryByUsername" parameterType="string" resultMap="userMap">
        SELECT id, user_id, username, password, name, email, phone, status, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>

    <select id="queryByUserId" parameterType="string" resultMap="userMap">
        SELECT id, user_id, username, password, name, email, phone, status, create_time, update_time
        FROM user
        WHERE user_id = #{userId}
    </select>

    <update id="update" parameterType="cn.bugstack.infrastructure.persistent.po.User">
        UPDATE user
        SET name = #{name}, email = #{email}, phone = #{phone}, status = #{status}, update_time = #{updateTime}
        WHERE user_id = #{userId}
    </update>

    <select id="queryPage" resultMap="userMap">
        SELECT * FROM user WHERE 1=1
        <if test='keyword != null and keyword != ""'>
            AND (username LIKE CONCAT('%',#{keyword},'%') OR phone LIKE CONCAT('%',#{keyword},'%'))
        </if>
        <if test='status != null and status != ""'>
            AND status=#{status}
        </if>
        ORDER BY create_time DESC LIMIT #{offset},#{limit}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM user WHERE 1=1
        <if test='keyword != null and keyword != ""'>
            AND (username LIKE CONCAT('%',#{keyword},'%') OR phone LIKE CONCAT('%',#{keyword},'%'))
        </if>
        <if test='status != null and status != ""'>
            AND status=#{status}
        </if>
    </select>

    <update id="updateStatus">
        UPDATE user SET status=#{status}, update_time=NOW() WHERE user_id=#{userId}
    </update>
</mapper>
