<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.persistent.dao.IAwardDao">
    <resultMap id="dataMap" type="cn.bugstack.infrastructure.persistent.po.Award">
        <id column="id" property="id"/>
        <result column="award_id" property="awardId"/>
        <result column="award_key" property="awardKey"/>
        <result column="award_config" property="awardConfig"/>
        <result column="award_desc" property="awardDesc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryAwardList" resultMap="dataMap">
        select award_id, award_key, award_config, award_desc
        from award
        limit 10
    </select>

    <select id="queryAwardConfigAwardId" resultType="java.lang.String" parameterType="java.lang.Integer">
        select award_config
        from award
        where award_id = #{awardId}
    </select>

    <select id="queryAwardKey" resultType="java.lang.String" parameterType="java.lang.Integer">
        select award_key
        from award
        where award_id = #{awardId}
    </select>

    <insert id="insert" parameterType="cn.bugstack.infrastructure.persistent.po.Award">
        INSERT INTO award(award_id, award_key, award_config, award_desc, create_time, update_time)
        VALUES(#{awardId}, #{awardKey}, #{awardConfig}, #{awardDesc}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="cn.bugstack.infrastructure.persistent.po.Award">
        UPDATE award SET award_key=#{awardKey}, award_config=#{awardConfig}, award_desc=#{awardDesc}, update_time=#{updateTime} WHERE award_id=#{awardId}
    </update>

    <delete id="deleteByAwardId">
        DELETE FROM award WHERE award_id=#{awardId}
    </delete>

    <select id="queryAwardById" resultMap="dataMap">
        SELECT * FROM award WHERE award_id=#{awardId}
    </select>

    <select id="queryAwardPage" resultMap="dataMap">
        SELECT * FROM award WHERE 1=1
        <if test='awardDesc != null and awardDesc != ""'>
            AND award_desc LIKE CONCAT('%',#{awardDesc},'%')
        </if>
        ORDER BY create_time DESC LIMIT #{offset},#{limit}
    </select>

    <select id="countAward" resultType="int">
        SELECT COUNT(*) FROM award WHERE 1=1
        <if test='awardDesc != null and awardDesc != ""'>
            AND award_desc LIKE CONCAT('%',#{awardDesc},'%')
        </if>
    </select>
</mapper>
